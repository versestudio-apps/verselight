<?xml version="1.0" encoding="utf-8"?>
<!-- VerseLight launch background. Uses brand cream so the splash transitions
     smoothly into the in-app scaffoldBackgroundColor (AppColors.cream). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/brand_cream" />

    <!-- TODO(phase-07+): place a centered brand wordmark or symbol here once
         design assets are produced (PNG in mipmap-* or vector in drawable-*).
         Example:
         <item>
             <bitmap
                 android:gravity="center"
                 android:src="@mipmap/launch_image" />
         </item>
    -->
</layer-list>
